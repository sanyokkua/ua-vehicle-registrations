package ua.vehicle.registrations.vehicle.management.repository;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class VehicleRegistrationRepositoryService {

    public static void main(String[] args) {
        SpringApplication.run(VehicleRegistrationRepositoryService.class, args);
    }
}
